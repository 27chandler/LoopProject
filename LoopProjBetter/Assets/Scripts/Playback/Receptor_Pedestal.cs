using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Receptor_Pedestal : MonoBehaviour
{
    [SerializeField] public string receptor_tag;
    [SerializeField] public Transform origin_transform;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
